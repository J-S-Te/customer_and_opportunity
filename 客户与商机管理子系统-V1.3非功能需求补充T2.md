# 客户与商机管理子系统 — V1.3 非功能需求补充（T2）

| 文档信息 | |
|---|---|
| 文档版本 | T2（建议稿） |
| 编制日期 | 2026-07-07 |
| 上游基线 | 《需求说明书 V1.2》（§三"非功能需求"已声明本期暂缓，留待 V1.3 补充）、《技术栈建议文档 T1》、《建表 DDL T3》 |
| 状态 | **建议（待安全/运维负责人确认）** |
| 编制角色 | 产品通 + 安全合规建议 |

> **Why**：V1.2 §三明确"加密算法、备份 RPO/容灾、等保测评等具体指标本期暂不设计实施，留待后续 V1.3 单独补充"。本系统是测评业务合同前的中枢，承载大量企业敏感信息（统一社会信用代码、银行账号、联系方式、测评报告），**等保三级测评是上线硬性门槛**。本文件补全这些指标，作为 T1/T3 的安全落地依据与 T8 测试验收的安全基线。

---

## 一、文档定位与范围

### 1.1 补全项（V1.2 中已声明暂缓）

| 暂缓项（V1.2 原文） | 本文件章节 |
|---|---|
| 客户敏感数据加密存储 | 第三章 数据安全与加密 |
| 备份与恢复（RPO/容灾） | 第四章 备份、恢复与容灾 |
| 符合网络安全等级保护三级要求 | 第二章 等保三级 |
| 性能/可用性仅保留 V1.1 基线 | 第五章 性能与容量、第六章 可用性与运维 |

### 1.2 范围边界

- **范围**：本子系统自身的应用安全、数据加密、备份容灾、性能容量、合规与审计。
- **非范围**：物理机房等保（依赖云/IDC 既有资质）、IM/邮件/短信三方（本期不接）、测评报告文件生成（CP-003 仅通道）。详见第十章 Non-goals。

---

## 二、网络安全等级保护三级（等保 2.0 第三级）

### 2.1 总体控制域映射

依据《信息安全技术 网络安全等级保护基本要求》（GB/T 22239-2019）第三级，本系统需覆盖的 10 个控制域及落地责任：

| 控制域 | 本系统落地要点 | 状态 |
|---|---|---|
| 安全物理环境 | 依托云/IDC 既有资质，不自建 | 复用 |
| 安全通信网络 | 全站 HTTPS（TLS 1.2+）、内网 mTLS | 本期落地 |
| 安全区域边界 | 反向代理 + API 网关隔离、WAF | 本期落地 |
| **安全计算环境** | 身份鉴别/访问控制/审计/入侵防范/数据加密 | **本期落地（核心）** |
| 安全管理中心 | 日志集中、统一审计、集中告警 | 本期落地 |
| 安全管理制度 | 文档化制度（本文件即一部分） | 配合 |
| 安全管理机构 | 角色职责（管理员/安全员/审计员分离） | 配合 |
| 安全管理人员 | 账号生命周期、权限最小化 | 本期落地 |
| 安全建设管理 | 上线前安全测试（见 T8） | 配合 |
| 安全运维管理 | 备份演练、漏洞扫描、监控告警 | 本期落地 |

### 2.2 安全计算环境详细要求（核心）

| 要求项（三级） | 具体指标 | 本系统落地方案 | 关联表/模块 |
|---|---|---|---|
| 身份鉴别 | 口令长度≥8、复杂度（大小写+数字+特殊字符）；失败 5 次锁 15 分钟；双因素 | `t_portal_account`（fail_count/lock_until/two_factor_enabled）；CP-001 | 门户 |
| 身份鉴别 | 口令定期更换、不回显 | 会话超时 30min 可配；密码 BCrypt 哈希存储 | 门户 |
| 访问控制 | 默认拒绝、角色最小化；敏感字段按角色脱敏 | RBAC（管理员/销售/财务/客户）；`phone`/`bank_account` 脱敏展示 | 全局 |
| 安全审计 | 覆盖重要用户行为和安全事件；日志不可篡改、定期备份 | `t_customer_log`/`t_customer_communication`/`t_portal_login_log`（永久留存）；追加写 | 全局 |
| 安全审计 | 审计记录含事件日期、用户、事件类型、结果 | 日志表字段全量覆盖 who/what/when/from-ip | 全局 |
| 入侵防范 | 限制非法登录、检测非法连接；非必要不开放多余端口 | 网关层限制；仅开放 443/业务端口 | 接入层 |
| 恶意代码防范 | 上传文件病毒扫描 | 附件上传经扫描（对象存储/网关层） | t_attachment |
| 数据完整性 | 重要数据校验（传输/存储防篡改） | 报告文件 SHA-256（`t_report_file.file_hash`）；传输 TLS | 报告 |
| 数据保密性 | 敏感数据存储/传输保密 | AES-256 字段加密 + HTTPS；报告文件 AES-256 + 水印 | 全局/报告 |
| 数据可用性 | 重要数据备份恢复 | 第四章备份策略；对象存储多副本 | 全局 |

### 2.3 安全管理中心与集中管控

- **统一日志**：应用审计日志 + Nginx 访问日志汇总至集中日志平台（Elastic/司内 SIEM），保留 ≥180 天。
- **集中告警**：异常登录（5 次锁定、异地 IP）、报告链接异常冻结、审批越权尝试触发实时告警。
- **权限三员分离**：系统管理员、安全管理员、审计管理员账号独立，互斥。

---

## 三、数据安全与加密

### 3.1 字段级加密（AES-256-GCM）

> 算法：AES-256-GCM（带认证标签，防篡改）。密钥由 KMS 托管，应用层不硬编码。

| 字段 | 所属表 | 存储方式 | 展示方式 |
|---|---|---|---|
| `phone` 联系电话 | t_customer_stakeholder | AES-256 密文 | 按角色脱敏（如 138****8000） |
| `bank_account` 收款账户 | t_customer_finance | AES-256 密文 | 仅财务/管理员可见明文（脱敏展示） |
| `tax_no` 税号 | t_customer_finance | AES-256 密文 | 按角色脱敏 |
| `contact_phone` 联系电话 | t_portal_account | AES-256 密文 | 仅本人/管理员 |
| `contact_email` 邮箱 | t_portal_account | AES-256 密文 | 按角色脱敏 |
| `password_hash` 密码 | t_portal_account | **BCrypt/Argon2 哈希**（非 AES） | 不可逆向 |
| `download_password` 解压密码 | t_report_request | AES-256 密文 | 单独渠道发送，不落库明文日志 |
| 备案 `contact_phone`/`credit_code` | t_filing_record | AES-256 密文 | 按角色脱敏 |
| 报告文件实体 | 对象存储 | AES-256 加密 + 水印（客户名+有效期） | 解密后下载 |

> **注意**：脱敏展示必须由应用层按角色控制；数据库直连查询只能得到密文，满足"即使数据泄露也不泄密"。

### 3.2 国密 SM4 预留

- 加密模块支持算法开关（`encrypt_algorithm = AES-256 | SM4`），默认 AES-256，满足等保"国密合规预留"要求。
- 切换 SM4 时仅改配置，不改业务代码（依赖 T1 既定"国密预留开关"决策）。

### 3.3 密钥管理（KMS）

| 项 | 要求 |
|---|---|
| 密钥存储 | 独立 KMS（司内 KMS 或云 KMS），禁止硬编码/配置文件明文 |
| 信封加密 | 主密钥（KMS）包裹数据密钥，数据密钥本地内存使用后即清 |
| 轮换周期 | 数据密钥 ≤ 90 天轮换；主密钥按 KMS 策略 |
| 权限 | 仅应用服务账号可调用解密接口；运维无密钥访问权限 |
| 备份 | 密钥备份遵循 KMS 既有容灾，不随业务库导出 |

### 3.4 传输安全

- 对外（WEB/PORTAL）：HTTPS，TLS 1.2+（优先 1.3），HSTS 开启。
- 对内（API ↔ MQ ↔ 下游）：内网隔离 + mTLS（服务间双向认证）。
- 邀请码/下载链接：一次有效（invite_used）、有效期（默认 7 天/72 小时）、随机不可猜。

---

## 四、备份、恢复与容灾

| 数据/组件 | RPO | RTO | 策略 | 保留期 |
|---|---|---|---|---|
| MySQL 业务库 | ≤ 15 分钟 | ≤ 4 小时 | 每日全量 + binlog 增量；备份文件 AES-256 加密 | ≥ 30 天 + 异地副本 |
| Redis 缓存 | 允许丢失（可重建） | — | 持久化 AOF；非关键 | — |
| 对象存储（报告/标书/附件） | ≈ 0 | ≤ 1 小时 | 多副本（≥3）× 跨可用区；版本控制防误删 | 与业务保留期一致 |
| 跨子系统消息 | 由 MQ 持久化保证 | — | RabbitMQ 镜像队列 + 死信 | 见 T5 |

- **异地灾备**：业务库每日全量同步至异地机房（与合同系统同区域策略）。
- **恢复演练**：每季度至少一次恢复演练，验证 RTO 达标，留痕归档。
- **备份安全**：备份文件同样 AES-256 加密；备份账号权限最小化。

---

## 五、性能与容量

### 5.1 性能指标（细化 V1.2 §3.1 基线）

| 指标 | 目标值 | 说明 |
|---|---|---|
| 页面响应时间 | ≤ 2 秒（P95） | 后台/门户交互 |
| 数据查询响应 | ≤ 1 秒（P95） | 客户/商机列表、详情 |
| 报表/导出生成 | ≤ 5 秒 | Excel 导出、备案 PDF |
| 客户检索（100+ 并发） | ≤ 2 秒 | CM-002 约束 |
| API 读接口 P99 | ≤ 300 ms | 对齐 T4 |
| API 写接口 P99 | ≤ 800 ms | 含加密/审计写入 |
| 并发用户 | ≥ 100 在线 | 销售 + 管理 + 客户门户 |

### 5.2 容量规划（基线假设，待业务确认）

| 维度 | 年增量假设 | 留存策略 |
|---|---|---|
| 客户档案 | ≤ 5 万/年 | 软删保留 6 年 |
| 商机 | ≤ 10 万/年 | 软删保留 6 年 |
| 报价单/投标 | 量级同商机 | 永久 |
| 报告文件 | ≤ 2 万/年 × 平均 20MB | 对象存储多副本 |
| 沟通/日志 | 高写入 | 永久；考虑冷热分离（>2 年归档） |

> 容量阈值达 70% 触发扩容评审；MySQL 全文检索超阈值（如客户量 > 50 万）切换 ES（见 T1 待确认）。

---

## 六、可用性与运维

| 指标 | 目标 | 说明 |
|---|---|---|
| 系统可用性 | ≥ 99.5% | V1.2 §3.4 基线 |
| 故障恢复时间 | ≤ 4 小时 | 对齐 V1.2 |
| 运行时间 | 7×24 | — |
| 健康检查 | `/healthz` 探活 | K8s/网关 |
| 监控告警 | Prometheus + 司内告警 | 应用/DB/MQ/Redis 指标 |
| 灰度发布 | 新版本按 10%→50%→100% | 降低上线风险 |

---

## 七、合规要求

- **《网络安全法》《数据安全法》《个人信息保护法》**：客户个人信息（联系方式、银行账号、信用代码）采集遵循"最小必要 + 告知同意"；提供脱敏与权限控制。
- **数据出境**：本期全部境内存储与处理，不涉及跨境传输。
- **个人信息主体权利**：支持查询、更正、删除（删除走软删，保留合规留存部分）；客户可通过门户反馈行权。
- **保密协议**：测评相关材料（标书、报告）按保密要求加密 + 权限管控。

---

## 八、日志与审计（落地对齐附录 A）

| 日志表 | 留存 | 内容 |
|---|---|---|
| t_customer_log | 永久 | 客户增删改/合并/信用调整/状态变更 |
| t_customer_communication | 永久 | 沟通记录 |
| t_portal_login_log | 永久 | 门户登录（IP/UA/状态） |
| t_report_issue_log | 永久 | 报告发放与下载（IP/时间） |
| t_customer_feedback / t_service_evaluation | 永久 | 反馈与评价 |
| 应用访问日志 | ≥ 180 天 | Nginx + 应用，集中日志平台 |

审计防篡改：审计类日志仅追加（append-only），业务层禁止 UPDATE/DELETE（由 ORM 层面限定）。

---

## 九、待确认事项

| 项 | 说明 | 建议 |
|---|---|---|
| 等保测评机构与排期 | 三级测评需第三方机构 | 上线前 1 个月启动，预留整改期 |
| KMS 选型 | 司内 KMS vs 云 KMS | 与合同系统一致 |
| 异地灾备机房 | 具体异地节点 | 复用司内既有异地策略 |
| 性能压测目标 TPS | 当前仅给响应时间/并发 | 补 TPS 基线（建议 ≥ 200 TPS 写） |
| 冷数据归档策略 | >2 年日志归档 | 归档至低成本存储，保留可查 |

---

## 十、Non-goals（本期不做）

- 不接 IM/邮件/短信三方（仅站内消息，字段预留）。
- 不生成测评报告文件（CP-003 仅通道）。
- 不管理项目执行（CP-002 仅拉快照）。
- 不建设物理机房等保（依赖云/IDC 既有资质）。
- 不做国密 SM4 默认开启（仅预留开关，默认 AES-256）。

---

**文档结束**
