# 新子系统开发对接交付清单

> 文档版本：V1.0  
> 更新日期：2026-07-31  
> 使用对象：基础平台负责人、业务子系统负责人、前端、后端、测试和运维人员  
> 文档用途：双方启动新子系统开发时填写并确认，作为接口联调和上线验收依据。  
> 权威运行手册：[`platform/docs/subsystem-onboarding.md`](../../platform/docs/subsystem-onboarding.md)

---

## 1. 对接目标

新子系统接入后应满足：

1. 用户从统一门户进入子系统，不直接访问子系统内网端口。
2. 子系统通过基础平台 OIDC 完成统一登录，不保存基础平台密码，不自行签发平台身份。
3. 子系统定义自己的角色、权限和数据范围，并将授权目录发布给基础平台。
4. 基础平台负责人员、组织和岗位的应用角色分配，并向目标应用签发隔离的角色、权限 Claims。
5. 子系统后端对每个业务接口执行身份、权限、租户和数据范围校验；前端隐藏菜单不能代替后端鉴权。
6. 子系统代码、镜像和业务功能的日常更新不改变接入关系，不重复执行 onboard/offboard。

### 1.1 责任边界

| 内容 | 基础平台负责 | 子系统负责 |
| --- | --- | --- |
| 用户、账号、组织、岗位、任职关系 | 主数据和管理能力 | 保存必要 ID 或业务快照，不复制密码体系 |
| 统一认证 | OIDC Issuer、授权端点、Token、JWKS、UserInfo | Authorization Code + PKCE 客户端、本地会话 |
| 应用登记 | Application、Environment、LoginTarget、OAuth Client | 提供应用编码、路径、回调和内部 Upstream |
| 业务授权 | 镜像授权目录、分配角色、按应用签发 Claims | 定义并发布角色/权限目录，执行最终鉴权和数据范围 |
| 门户入口 | 门户目录、统一公开地址、Nginx 路由 | 子路径兼容、健康检查和内部服务可达性 |
| 业务数据 | 不保存子系统业务数据 | 独立数据库、迁移、备份和数据隔离 |
| 业务审计 | 接收已授权的标准审计事件 | 产生业务审计事件并避免泄露敏感信息 |
| 日常发布 | 保持现有登记稳定 | 自己的 CI/CD、镜像、迁移和回滚 |

---

## 2. 双方启动会需要填写的信息

以下表格填写完整后再开始联调。

### 2.1 应用登记信息

| 字段 | 约束 | 双方确认值 |
| --- | --- | --- |
| 应用编码 `application_code` | 全局唯一；匹配 `^[a-z][a-z0-9._-]{0,63}$`；建议与项目目录同名 | `________________` |
| 应用名称 `application_name` | 门户显示的中文名称 | `________________` |
| 应用说明 `description` | 简述业务范围，不填写敏感信息 | `________________` |
| 环境 `environment` | `dev/test/staging/prod` | `________________` |
| 对外入口 `public_base_url` | 浏览器访问的门户 origin，不带业务路径 | `________________` |
| 路径前缀 `path_prefix` | 非根绝对路径，例如 `/customer_management` | `________________` |
| 内部地址 `upstream_url` | Nginx/Docker 网络可访问地址，不能误用其他容器的 `localhost` | `________________` |
| OAuth Client 类型 | 服务端 Web 应用推荐 `confidential` | `________________` |
| 平台管理 API | 本机推荐 `http://127.0.0.1:8081/api/v1` | `________________` |
| 平台可信 Origin | 与门户公开 origin、平台 CORS/同源配置一致 | `________________` |
| 子系统技术负责人 | 姓名和联系方式 | `________________` |
| 基础平台对接人 | 姓名和联系方式 | `________________` |

平台根据上述信息自动派生：

```text
公开业务地址 = <public_base_url><path_prefix>/
OIDC 回调地址 = <public_base_url><path_prefix>/auth/callback
浏览器 Client ID = <application_code>-<environment>-web
登录目标 = <path_prefix>/
```

### 2.2 前端承载模式

双方必须二选一，不能同时配置两种整站路由。

| 模式 | 适用情况 | 选择 |
| --- | --- | --- |
| 统一前端模式 | Vue 模块放入现有 `frontend`，与基础平台前端共同构建为一个 Docker 镜像；Nginx 只代理该子系统的 `/api/`、`/auth/`、`/healthz` 等后端路径 | `[ ]` |
| 独立前端模式 | 子系统自己提供完整前端，Nginx 将整个路径前缀代理至子系统 Upstream | `[ ]` |

> 当前项目的合同管理采用统一前端模式。若新系统也使用统一前端，不能直接套用“整站反向代理”规则覆盖 Vue 路由；接入前由基础平台负责人确认 provisioner/Nginx 配置方式。

---

## 3. 子系统必须实现的 OIDC 契约

### 3.1 必要端点

子系统后端至少提供：

| 方法 | 对外路径 | 作用 |
| --- | --- | --- |
| `GET` | `<path_prefix>/auth/login` | 生成 state、nonce、PKCE 并跳转平台授权端点 |
| `GET` | `<path_prefix>/auth/callback` | 校验回调、交换 Token、创建本地会话 |
| `GET` 或 `POST` | `<path_prefix>/auth/logout` | 撤销子系统会话并清除 Cookie |
| `GET` | `<path_prefix>/api/v1/auth/me` | 返回当前子系统身份、角色、权限和会话信息 |
| `GET` | `<path_prefix>/healthz` | 部署和网关健康检查 |

内部 Upstream 收到的路径是否移除 `path_prefix`，由双方在 Nginx 规则中明确；子系统不得自行猜测。

### 3.2 登录流程要求

- [ ] 使用 OIDC Authorization Code Flow。
- [ ] 使用 PKCE S256，不允许 `plain`。
- [ ] `state`、`nonce`、`code_verifier` 使用安全随机数。
- [ ] state 服务端保存或防篡改绑定，并且只能消费一次。
- [ ] Token 交换在子系统后端完成，Client Secret 不进入浏览器。
- [ ] 通过 `<OIDC_ISSUER>/.well-known/openid-configuration` 获取协议端点。
- [ ] JWKS 只来自已信任 Issuer，不接受 Token 传入的任意密钥地址。
- [ ] 校验签名算法、`kid`、`iss`、`aud`、`exp`、`iat`、`nonce` 和 `token_use=id_token`。
- [ ] 校验 `sub`、`tenant_id`、`roles`、`permissions`、`role_config_hash`、`authz_revision`。
- [ ] 拒绝未知角色、未知权限、跨应用权限和不兼容的角色目录哈希。
- [ ] OIDC 失败时返回受控错误，不向浏览器显示 Secret、Token 或内部堆栈。

### 3.3 本地会话要求

- [ ] 子系统建立自己的服务端会话，不共享或解析平台 `bp_session` Cookie。
- [ ] Cookie 使用独立名称，例如 `<application_code>_session`。
- [ ] Cookie 至少设置 `HttpOnly`、合理的 `SameSite` 和精确 `Path=<path_prefix>`。
- [ ] 生产 HTTPS 设置 `Secure=true`。
- [ ] 会话有效期不得超过关联 Token 有效期；建议默认最长 15 分钟。
- [ ] 权限变更、账号禁用或平台会话失效后，子系统通过 UserInfo 定期复核或重新登录使旧授权失效。
- [ ] 登出撤销服务端会话，不能只删除前端内存状态。
- [ ] 写接口具备 CSRF/严格同源防护。

建议的 `/api/v1/auth/me` 响应业务数据：

```json
{
  "user_id": "<OIDC sub>",
  "tenant_id": "<tenant id>",
  "display_name": "张三",
  "roles": ["sales"],
  "permissions": ["dashboard", "customer.read"],
  "role_config_hash": "<opaque compatible hash>",
  "authz_revision": 3,
  "expires_at": "2026-07-31T12:00:00Z"
}
```

接口的外层响应格式应遵循项目统一结构：

```json
{
  "code": "OK",
  "message": "success",
  "request_id": "<request-id>",
  "data": {}
}
```

认证错误约定：未登录返回 `401`，已登录但权限不足返回 `403`。

---

## 4. 身份与业务数据映射

子系统不得以账号名、手机号或显示名称作为稳定身份主键。建议所有需要追踪所有者和操作人的业务表保存：

```text
tenant_id
platform_user_id       # OIDC sub
created_by
updated_by
created_at
updated_at
```

双方确认：

| 项目 | 约定值 |
| --- | --- |
| 子系统用户稳定标识 | OIDC `sub` / `platform_user_id` |
| 租户隔离字段 | `tenant_id` |
| 是否需要组织 ID | `是 / 否` |
| 是否需要岗位 ID | `是 / 否` |
| 是否需要任职关系 ID | `是 / 否` |
| 用户展示信息来源 | `OIDC/UserInfo/受控同步/业务快照` |
| 账号禁用后的会话失效时限 | `________________` |

要求：

- 不复制基础平台密码、密码哈希、平台 Cookie 或认证失败记录。
- 每个业务查询和写入都校验当前 `tenant_id`。
- 展示名称可以保存快照，但不能代替 `platform_user_id` 建立关联。
- 如需组织、岗位和任职数据，应按最小权限使用受控接口或同步机制，不直接读取平台数据库。

---

## 5. 角色、权限与数据范围交付物

### 5.1 权限清单

子系统负责人需提交完整权限表。权限编码投入使用后不得随意改名。

| 权限编码 | 中文名称 | 资源 | 动作 | 风险级别 | 对应 API/功能 |
| --- | --- | --- | --- | --- | --- |
| `________________` | `________________` | `________________` | `read/create/update/delete/process/...` | `LOW/MEDIUM/HIGH/CRITICAL` | `________________` |
| `________________` | `________________` | `________________` | `________________` | `________________` | `________________` |

### 5.2 角色清单

| 角色编码 | 角色名称 | 默认权限 | 是否可分配 | 说明 |
| --- | --- | --- | --- | --- |
| `________________` | `________________` | `________________` | `是/否` | `________________` |
| `________________` | `________________` | `________________` | `是/否` | `________________` |

### 5.3 授权策略

| 策略 | 双方确认值 |
| --- | --- |
| 一个用户最多有效角色数 `max_effective_roles` | `________________`；`0` 表示平台通用模型不限制 |
| 是否允许组织继承角色 | `是 / 否` |
| 是否允许岗位模板继承角色 | `是 / 否` |
| 是否允许用户直接分配角色 | `是 / 否` |
| 是否支持用户直接附加权限 | 默认 `否`；通用目录使用角色分配 |
| 超级管理员通配权限编码 | `________________ / 不使用` |
| 角色配置兼容哈希算法与规范化规则 | `________________` |
| 授权变更最长生效时间 | `________________` |

### 5.4 数据范围矩阵

权限只描述“能做什么”，数据范围描述“能对谁的数据做”。子系统必须在服务层明确实现。

| 角色 | 资源 | 查看范围 | 修改范围 | 删除范围 | 特殊限制 |
| --- | --- | --- | --- | --- | --- |
| `________________` | `________________` | `本人/组织/全部` | `本人/组织/全部/无` | `本人/组织/全部/无` | `________________` |

即使给用户增加编辑权限，也不能自动扩大其“仅本人”数据范围。

---

## 6. 授权目录发布契约

子系统是自己角色和权限目录的唯一定义方；基础平台保存只读镜像并负责分配。

一键接入会为目录同步生成独立的机器 Client。它不能与浏览器 OIDC Client、审计 Client 混用。

```http
PUT /api/v1/applications/{application_id}/authorization-catalog
Authorization: Bearer <client_credentials access token>
Content-Type: application/json
```

机器 Token 必须具备：

```text
scope = authorization.catalog.sync
token_use = application
application_id = 路径中的 application_id
```

目录示例：

```json
{
  "catalog_version": "customer-management-v1",
  "checksum": "sha256:<按平台规范计算的校验值>",
  "claims_role_config_hash": "sha256:<子系统本地角色配置兼容哈希>",
  "permissions": [
    {
      "code": "customer.read",
      "name": "查看客户",
      "action": "read",
      "resource_code": "customer",
      "resource_name": "客户",
      "risk_level": "LOW"
    }
  ],
  "roles": [
    {
      "code": "sales",
      "name": "销售人员",
      "description": "维护本人负责的客户",
      "permissions": ["customer.read"]
    }
  ],
  "policy": {
    "max_effective_roles": 1
  }
}
```

实现要求：

- [ ] 目录版本和 checksum 稳定、可重复计算。
- [ ] 权限编码不重复；同一 `resource_code + action` 不重复。
- [ ] 角色引用的权限都存在。
- [ ] `risk_level` 仅使用 `LOW/MEDIUM/HIGH/CRITICAL`。
- [ ] 子系统启动或发布时执行幂等同步；失败记录告警并可安全重试。
- [ ] Secret 只存在后端 Secret 配置，不进入前端、Git、命令行或日志。
- [ ] 目录删除角色/权限前制定存量绑定和兼容策略。
- [ ] 同步成功后，基础平台用户详情和岗位授权模板能够读取该应用角色。

详细规则参见 [`应用授权目录同步说明`](../05_能力模块/应用授权目录同步说明.md)。

---

## 7. 前端对接要求

### 7.1 通用要求

- [ ] 前端启动时调用子系统自己的 `<path_prefix>/api/v1/auth/me`，不调用基础平台 `/api/v1/auth/me` 代替子系统会话。
- [ ] 401 跳转 `<path_prefix>/auth/login`，403 展示无权限页。
- [ ] 菜单、路由和按钮根据 `permissions` 展示。
- [ ] 直接输入 URL 时执行路由权限检查。
- [ ] 所有敏感业务 API 仍由后端重新鉴权。
- [ ] 请求使用同源 HttpOnly Cookie，不把 Token 写入 `localStorage/sessionStorage`。
- [ ] API 调用集中封装，统一处理 `code/message/request_id`。
- [ ] 页面不硬编码演示账号、角色或权限。
- [ ] 子路径刷新、静态资源、懒加载 chunk 和重定向均正常。

### 7.2 统一前端模式附加要求

模块建议放置于：

```text
frontend/src/modules/<application_code>/
├── api/
├── components/
├── views/
├── styles/
├── utils/
├── module.js
└── README.md
```

需要共同修改/确认：

- `frontend/src/modules/moduleRegistry.js` 的展示元数据；
- `frontend/src/router/index.js` 的子路径路由和会话守卫；
- `frontend/nginx/default.conf` 或生成的 include，仅代理后端路径；
- API 前缀统一为 `<path_prefix>/api/v1`；
- 统一 frontend Docker 构建包含新模块。

> `module.js` 只提供本地展示信息，不能代替基础平台 Application/Environment 登记。

### 7.3 独立前端模式附加要求

- Vite/Webpack public base 与 Router base 使用 `<path_prefix>/`。
- 子系统自身 Upstream 提供 SPA fallback。
- Nginx 可以将整个 `<path_prefix>/` 代理至子系统。
- 不把浏览器重定向到内部 `upstream_url`。

---

## 8. Docker、网络和配置交付物

子系统项目目录建议与 `application_code` 同名，并至少提供：

```text
<application_code>/
├── compose.yaml
├── Dockerfile
├── .env.example
├── migrations/
├── README.md
└── ...
```

交付检查：

- [ ] Compose 可通过 `docker compose ... up -d --build` 启动。
- [ ] 提供 `.env.example`，真实 `.env.local` 不提交 Git。
- [ ] `.env.local` 权限为 `0600`。
- [ ] 后端加入平台网关可访问的 Docker 网络。
- [ ] Upstream 使用 Docker 服务名和容器端口。
- [ ] 子系统数据库独立，迁移可重复、安全且有回滚/恢复方案。
- [ ] `/healthz` 覆盖必要依赖状态。
- [ ] 后端端口不直接暴露公网。
- [ ] 日志不记录密码、Cookie、授权码、Access Token、ID Token、Refresh Token 或 Client Secret。
- [ ] 提供启动、升级、备份、恢复和故障排查 README。

接入脚本会写入或托管的标准变量：

```dotenv
PLATFORM_BASE_URL=
OIDC_ISSUER=
OIDC_BACKCHANNEL_BASE_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=
OIDC_SCOPES=openid profile
OIDC_TENANT_ID=

APP_PUBLIC_URL=
APP_PATH_PREFIX=
PLATFORM_APPLICATION_ID=
PLATFORM_APPLICATION_CODE=
PLATFORM_ENVIRONMENT_CODE=
PLATFORM_DOCKER_NETWORK=

PLATFORM_AUTHORIZATION_CATALOG_SYNC_ENABLED=true
PLATFORM_AUTHORIZATION_CATALOG_CLIENT_ID=
PLATFORM_AUTHORIZATION_CATALOG_CLIENT_SECRET=
PLATFORM_AUTHORIZATION_CATALOG_APPLICATION_ID=
```

`OIDC_BACKCHANNEL_BASE_URL` 可让容器内 Token/UserInfo 请求走内部地址，但 ID Token 的公开 `iss` 仍必须严格匹配 `OIDC_ISSUER`。

---

## 9. 可选平台能力

双方根据业务范围勾选；未勾选的能力不属于本次交付。

| 能力 | 是否接入 | 子系统需要提交的内容 |
| --- | --- | --- |
| 组织、岗位、任职关系 | `[ ]` | 使用场景、所需字段、同步或查询方式、最小权限 |
| 岗位授权模板 | `[ ]` | 角色目录已同步、岗位到应用角色的映射规则 |
| 统一字典 | `[ ]` | 字典编码、维护方、缓存和停用策略 |
| 站内通知 | `[ ]` | 事件、接收人、模板、业务跳转路径、幂等键 |
| 审计事件上报 | `[ ]` | 操作编码、资源、结果、request_id、脱敏规则 |
| 跨子系统调用 | `[ ]` | 服务身份、audience、scope、超时、幂等和重试策略 |

每种机器用途必须使用独立 OAuth Client。例如审计 Client 只授予 `audit.ingest`，不能复用浏览器或目录发布 Client。

---

## 10. 首次接入与日常发布

### 10.1 Dry-run 模板

在项目根目录的 `platform/` 执行：

```bash
bash scripts/subsystem.sh onboard \
  --application-code '<application_code>' \
  --application-name '<应用名称>' \
  --description '<应用说明>' \
  --environment dev \
  --public-base-url 'http://localhost:8081' \
  --upstream-url 'http://<service-name>:<container-port>' \
  --path-prefix '/<application_code>' \
  --client-type confidential \
  --api-base-url 'http://127.0.0.1:8081/api/v1' \
  --platform-origin 'http://localhost:8081' \
  --dry-run --yes
```

### 10.2 首次正式接入

```bash
bash scripts/subsystem.sh onboard \
  --application-code '<application_code>' \
  --application-name '<应用名称>' \
  --description '<应用说明>' \
  --environment dev \
  --public-base-url 'http://localhost:8081' \
  --upstream-url 'http://<service-name>:<container-port>' \
  --path-prefix '/<application_code>' \
  --client-type confidential \
  --api-base-url 'http://127.0.0.1:8081/api/v1' \
  --platform-origin 'http://localhost:8081' \
  --account '<平台管理员账号>'
```

`--account` 是调用基础平台控制面的管理员账号，不是子系统业务账号。密码通过安全交互输入，不写到命令行。

### 10.3 日常更新

接入成功后，业务代码、镜像、功能模块和数据库迁移更新不需要重新接入：

```bash
bash scripts/subsystem.sh update \
  --application-code '<application_code>' \
  --environment dev \
  --account '<平台管理员账号>'
```

也可使用子系统自己的 CI/CD。只有永久下线才使用 offboard；不要为了重新部署删除 Application、Environment 或 OAuth Client。

---

## 11. 联调与安全验收

### 11.1 门户和网关

- [ ] 门户只出现一个正确的子系统卡片。
- [ ] 卡片跳转到 `<public_base_url><path_prefix>/`。
- [ ] 浏览器不暴露内部 Upstream 和子系统容器端口。
- [ ] `/healthz` 返回成功，Nginx 能访问 Upstream。
- [ ] 子路径静态资源、API、回调、重定向和页面刷新正常。

### 11.2 OIDC

- [ ] 未登录访问业务页会跳转统一登录。
- [ ] 平台已有会话时可无感完成 OIDC 回调。
- [ ] 回调后返回原业务页。
- [ ] 错误/重放 state 被拒绝。
- [ ] 错误 nonce、issuer、audience、签名、token_use 和过期 Token 被拒绝。
- [ ] 回调 URI 必须精确匹配登记值。
- [ ] 子系统登出后本地会话失效。
- [ ] 单终端账号约束和退出行为符合平台规则。

### 11.3 授权

- [ ] 授权目录状态为 `SYNCED`。
- [ ] 基础平台用户详情能读取该子系统角色。
- [ ] 岗位授权模板能读取目标应用和角色（如本次启用）。
- [ ] Token 只包含目标应用角色和权限，不泄露其他应用权限。
- [ ] 无角色用户不能进入子系统。
- [ ] 多角色冲突符合 `max_effective_roles` 策略。
- [ ] `/auth/me` 返回当前角色、权限、哈希、修订号和到期时间。
- [ ] 角色权限变更在双方约定的时限内生效。
- [ ] 前端菜单、路由、按钮与后端接口权限一致。

### 11.4 数据范围

- [ ] 不同租户之间无法读取、修改或枚举数据。
- [ ] “仅本人”角色无法操作其他人的数据。
- [ ] 增加操作权限不会绕过所有权/组织范围。
- [ ] IDOR、批量接口、导出和附件下载均执行相同数据范围校验。

### 11.5 安全与运维

- [ ] Secret 未提交 Git，运行配置权限正确。
- [ ] 日志和错误响应不包含敏感凭据或内部堆栈。
- [ ] 写操作具有 CSRF/同源保护和幂等策略。
- [ ] 关键业务操作有审计记录和 request_id。
- [ ] 数据库迁移、备份、恢复和回滚已演练。
- [ ] 健康检查、启动顺序和依赖失败行为符合预期。

---

## 12. 问题单和变更管理

联调问题统一携带：

```text
发生时间：
环境：
公开访问 URL：
application_code / environment：
request_id / 追踪号：
HTTP 方法和路径：
HTTP 状态码与业务 code：
期望结果：
实际结果：
相关服务日志（已脱敏）：
```

以下变更必须提前通知双方，并经过配置变更而不是删除重建：

- Public BaseURL；
- PathPrefix；
- UpstreamURL；
- OIDC Redirect URI / Post Logout URI；
- OAuth Client 凭据轮换；
- 角色编码、权限编码、角色权限关系；
- `claims_role_config_hash` 规范；
- 单/多角色策略；
- 会话有效期和授权刷新策略。

---

## 13. 双方交付确认

| 交付物 | 负责人 | 完成日期 | 状态/备注 |
| --- | --- | --- | --- |
| 应用登记信息 | `________` | `________` | `________` |
| 前端承载模式与路由设计 | `________` | `________` | `________` |
| OIDC 登录及本地会话 | `________` | `________` | `________` |
| 权限、角色和数据范围矩阵 | `________` | `________` | `________` |
| 授权目录发布 | `________` | `________` | `________` |
| Docker/网络/健康检查 | `________` | `________` | `________` |
| 前端权限菜单和路由守卫 | `________` | `________` | `________` |
| 审计/通知/组织等可选能力 | `________` | `________` | `________` |
| 联调测试报告 | `________` | `________` | `________` |
| 上线及回滚方案 | `________` | `________` | `________` |

基础平台负责人确认：`________________`  日期：`________________`  
子系统负责人确认：`________________`  日期：`________________`

---

## 14. 相关文档

- [子系统开发与统一身份接入手册](../../platform/docs/subsystem-onboarding.md)
- [子系统快速接入开发手册](子系统快速接入开发手册.md)
- [应用授权目录同步说明](../05_能力模块/应用授权目录同步说明.md)
- [JWT 文档](../03_接口规范/JWT文档.md)
- [平台接口规范](../03_接口规范/平台接口规范.yaml)
- [系统集成接口调用手册](../03_接口规范/系统集成接口调用手册.md)

