# 角色、权限与数据范围

## 1. CRM 角色

| 角色编码 | 名称 | 默认数据范围 | 关键权限 |
| --- | --- | --- | --- |
| sales | 销售 | SELF | customer/opportunity 读写、presale.create |
| sales_director | 销售总监 | ALL 或 ORG | 客户商机管理、TS 节点1审批、全量查看 |
| team_lead | 团队负责人 | ALL 或 ORG | TS 节点2审批、指派/改派、全量查看 |
| technician | 技术人员 | ASSIGNED | TS 详情、进度、工时 |
| technical_lead | 技术负责人 | ALL | TS 全量只读、报表、异常重试（按授权） |
| customer_admin | 客户管理员 | ALL | 客户合并/作废/导入，不自动拥有敏感明文 |
| auditor | 审计员 | ALL_READONLY | 审计只读 |

`max_effective_roles` 建议 3，最终由双方启动会确认。组织继承/岗位模板可启用，用户直接附加权限默认禁用。

## 2. CRM 权限目录

| 权限 | 风险 | 功能 |
| --- | --- | --- |
| customer.read/create/update | LOW/MEDIUM | 客户查询和维护 |
| customer.import/merge/void/restore | HIGH | 批量、合并和状态变更 |
| customer.export | HIGH | 普通字段导出 |
| customer.sensitive.read/export | CRITICAL | 敏感明文查看/导出，分开授权 |
| customer.audit.read | HIGH | 客户变更日志 |
| portal_account.provision/revoke | HIGH | 开通、重发、撤销邀请 |
| opportunity.read/create/update | LOW/MEDIUM | 商机主档 |
| opportunity.stage.change | HIGH | 无审批但必须审计的阶段调整 |
| opportunity.void/restore | HIGH | 商机作废恢复 |
| opportunity.contract.transfer | HIGH | 推送合同系统 |
| presale.read/create | LOW/MEDIUM | TS 查看和申请 |
| presale.approve | HIGH | 当前节点审批，角色规则二次校验 |
| presale.assign | HIGH | 指派/改派 |
| presale.progress/worklog | MEDIUM | 当前执行人操作 |
| presale.worklog.retry | HIGH | PMS 失败重试 |
| presale.alert.config/report | HIGH | 配置与报表 |

## 3. Portal 角色

外部客户在基础平台 Portal 应用授权目录中分配 `portal_customer`，OIDC Token 只携带 Portal 目标应用角色/权限。Portal 根据 Token `sub` 查询本地 ACTIVE 映射取得 customer_id，数据范围强制 CUSTOMER，不能由请求参数改变。若提供反馈处理或备案解锁后台，使用独立内部角色 `portal_operator/portal_admin`，不能赋给外部账号。

| 权限 | 范围 |
| --- | --- |
| project.read/export | 本客户项目 |
| project.message.read/send | 本账号在本客户项目中的当前经理站内会话 |
| report.request/read/download | 本客户报告 |
| filing.create/update/submit/read | 本客户备案 |
| evaluation.create/read | 本客户项目 |
| feedback.create/read/reply | 本客户反馈 |
| account.security.manage | 当前账号 |
| filing.unlock / feedback.process | 仅内部管理身份 |

## 4. 后端数据范围实现

统一 `Scope` 对象：`TenantID,Mode,UserID,OrgIDs,CustomerID,ResourceIDs`。Repository 只接受 Scope，不接受裸 user-supplied owner/customer 条件。查询构造器必须针对每个资源实现；批量、导出、附件、回调后的文件下载使用相同规则。

## 5. 授权目录交付

CRM 与 Portal 分别生成稳定 `catalog_version/checksum/claims_role_config_hash`，分别使用独立 `authorization.catalog.sync` 机器 Client 发布。Portal 的 `portal_customer` 由 CRM 开通流程通过基础平台受控管理接口分配，但角色定义权仍属于 Portal。角色/权限删除前必须检查存量绑定并提供迁移，不得直接重命名已上线编码。

验收：无角色用户不能进入；Token/会话只包含目标应用权限；前端路由、按钮与后端接口矩阵一致；权限变更在约定时限内生效。
