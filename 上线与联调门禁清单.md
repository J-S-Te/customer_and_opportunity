# 客户与商机管理系统上线与联调门禁清单

更新时间：2026-08-06

本清单把代码已经具备的能力、必须完成的外部联调和上线前业务决策分开。任何一项 P0 未关闭，不应将 CRM/售前标记为正式生产可用。

## P0：必须先关闭

### 1. 业务口径冻结

- [ ] 明确商机阶段推进、商机作废、终态重开是否需要销售总监审批；当前 PRD 与实现口径不一致。
- [ ] 明确门户账号初始密码责任；当前旧 PRD 仍写固定密码，新实现要求由基础平台管理员初始化，禁止两套口径并存。
- [ ] 明确“转合同”的成功定义：仅事件受理、合同草稿已创建，还是合同已完成关联。
- [ ] 明确已签约但暂未关联合同的统计口径，以及失败原因字典和重开规则。
- [ ] 由产品、架构、安全共同签字，回写 PRD、OpenAPI、权限目录、前端提示和验收用例。
- [ ] 验收客户、商机、售前申请和客户合并流程：关联 ID 只能由当前权限范围内的选择器/详情上下文产生；禁止文本框手填或仅凭 URL 参数写入，服务端仍需做租户、可见性和版本校验。

### 2. 外部系统联调

每个依赖必须有正式 endpoint、OAuth client、scope、证书/CA、字段契约、超时重试策略、测试数据和责任人。

- [ ] 基础平台 OIDC、人员/组织目录：生产 HTTPS、mTLS、角色与组织 scope、人员失效同步。
- [ ] 审批引擎：当前任务查询、审批动作、回调乱序/重放、审批人身份和组织归属。
- [ ] PMS：`person_id` 权威映射、人员同步、工时写入、幂等、死信和人工重试。
- [ ] 报价/投标：状态回传、金额查询、固定调起地址、签名/nonce 防重放、商机归属校验。
- [ ] 合同系统：合同编号校验、签单事件受理、客户/商机关联和异常补偿。
- [ ] 对象存储/病毒扫描：商机附件上传、最终化、扫描回调和受控下载。

### 3. 数据库与发布

- [ ] 在上一版本真实数据库执行全部 CRM/Portal 增量 migration。
- [ ] 验证 metadata lock、在线 DDL、复制延迟、长事务和大数据量索引性能。
- [x] migration 文件、发布计划、checksum、开发状态和回滚说明已同步；CI 已校验当前计划，但真实存量升级与回滚仍未验收。
- [ ] 完成备份恢复、应用回滚、数据库兼容性和灰度发布演练。

## P1：上线前必须有计划和负责人

- [ ] CRM 商机漏斗、赢单/输单率、预测和销售目标报表。
- [ ] 客户分层、重点客户权威分类、公海/认领、负责人交接 SLA。
- [ ] 客户导入病毒/内容扫描、导出 Worker、对象存储和短效下载。
- [ ] 外部 IM/邮件/短信通知，或明确站内消息的业务 SLA 和升级机制。
- [ ] 售前报表异步导出、聚合数据新鲜度、规模性能和导出权限。
- [ ] 联系电话明文查看的隐私评审；默认脱敏、临时授权和审计应优先于长期明文可见。

## 工程质量门禁

- [x] Go 单元测试和 `go vet`。
- [x] CRM/Portal OpenAPI 路由防漂移检查。
- [x] 前端 `npm test` 和 production build 纳入 CI。
- [ ] Vue 组件挂载级测试和关键流程浏览器 E2E。
- [ ] MySQL 集成测试、并发、P95、200 TPS 写入基线。
- [ ] SAST、依赖漏洞、OWASP ZAP 和 secret scanning。
- [x] `/livez`、`/readyz` 探针，并纳入 OpenAPI 路由契约。
- [ ] 指标、Worker/outbox/dead-letter 监控和告警。

## 验收证据归档

每项关闭必须归档：契约版本、环境地址、测试数据、请求/响应摘要、错误重试记录、权限矩阵结果、性能报告和责任人签字。仅通过单元测试或本地 mock 不得标记为“正式联调完成”。
