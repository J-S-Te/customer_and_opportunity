# 站内信双写平台 · 联调验收清单

> 覆盖两条链路：**合同（P0）** 与 **CRM（P1）** 的站内信投递到基础平台中央收件箱。
> 门户（P2）已确认不适用：门户通知受众是外部客户（AccountID/CustomerID），不是平台内部用户。

## 0. 前置条件

- [ ] 平台已执行 `subsystem_onboarding`，合同与 CRM 均已下发 `notification-publisher`（scope `notification.ingest`）凭据。
- [ ] 合同 `runtime` 环境写入 `PLATFORM_NOTIFICATION_CLIENT_ID/SECRET`。
- [ ] CRM `runtime/customer.env` 写入 `PLATFORM_NOTIFICATION_CLIENT_ID/SECRET`（模板已加 `PENDING_ONBOARDING` 占位）。
- [ ] 合同已执行迁移 `000019`（`con_notification_outbox` 增加 `locked_at`）。
- [ ] CRM 已执行迁移 `000098`（`crm_notifications` 增加 `platform_delivered_at`）。
- [ ] CRM 镜像已包含 `notification-delivery-worker` 二进制（Dockerfile 构建列表已加）。
- [ ] `docker compose config` 通过，`customer-notification-delivery-worker` 服务在 `customer` profile 下可启动。

## 1. 合同链路（P0）

- [ ] 发起一份合同审批，审批通过后：
  - [ ] `con_notification_outbox` 出现 `delivery_status='pending'` 的审批通知记录。
  - [ ] 通知投递 worker（API 进程内 `notification.Dispatcher`）将记录推进到 `delivered`，`last_error` 为空。
  - [ ] 平台 `notification_message` / `notification_delivery` 出现对应站内信，收件人解析正确。
- [ ] 自动归档定时任务触发后，`con_notification_outbox` 出现 `status_change` 通知并被投递。
- [ ] `role:` 收件人（如 `contract_specialist`）经 `owner-directory` 正确展开为多个用户，每人各收一条。
- [ ] 停掉平台通知端点，通知记录进入 `pending` 重试（`next_attempt_at` 递增），恢复后最终 `delivered`。
- [ ] 超过最大尝试次数后进入 `dead`，不再无限重试。

## 2. CRM 链路（P1）

- [ ] 触发商机负责人变更，`crm_notifications` 写入后，`notification-delivery-worker` 将其 `platform_delivered_at` 置为当前时间。
- [ ] 触发售前指派 / 售前进度事件，同上。
- [ ] 平台中央收件箱可见对应通知（`event_type` 为 `OPPORTUNITY_OWNER_CHANGED` / `PRESALE_*`）。
- [ ] `PRESALE_APPROVAL_PENDING` 通知在平台侧 `priority` 为 `HIGH`。
- [ ] 平台通知端点不可用时，worker 记录错误日志、不崩溃、下轮重试（幂等，平台按 `source_event_id` 去重）。
- [ ] 已 `CANCELLED` 的本地通知不会被上送。

## 3. 平台侧验收

- [ ] 用户登录平台，前端「通知中心」能看到来自合同 / CRM 的业务通知。
- [ ] 通知点击「查看关联内容」跳转到对应子系统相对路径（`/contract_management/contracts/{id}`、`/customer-opportunity/opportunities?...`），且目标系统重新鉴权。
- [ ] 未读计数正确；标记已读 / 全部已读生效。
- [ ] 同一业务事件重放不产生重复站内信（幂等键 `application:environment:idempotency_key`）。

## 4. 安全与边界

- [ ] 投递仅用 `notification.ingest` scope 机器令牌，未复用浏览器/审计/目录凭据。
- [ ] 投递请求未携带用户会话 Cookie/Bearer（平台侧按凭据推导 tenant/source）。
- [ ] `target_url` 为相对路径，未出现 `//` 或换行注入。
- [ ] 平台收件箱按 `tenant_id + user_id` 隔离，跨租户不可见。

## 5. 回滚与监控

- [ ] 未配置平台通知凭据时，合同侧不启动投递 goroutine、CRM worker 直接退出（fail-closed，不影响本地收件箱）。
- [ ] `notification_delivery_status='dead'` / `last_error` 有可观测告警路径（建议接入现有监控）。
- [ ] 回滚方案：关闭投递不影响本地 `crm_notifications` / 合同审批流程，仅平台中央收件箱停止新增。
