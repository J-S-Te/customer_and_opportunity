# 客户与商机管理子系统 — 前端原型与 UI 规范（T7）

| 文档信息 | |
|---|---|
| 文档版本 | T7（建议稿） |
| 编制日期 | 2026-07-07 |
| 上游基线 | 《技术栈建议文档 T1》《API 接口契约 T4》《需求说明书 V1.2》《建表 DDL T3》《V1.3 非功能需求补充 T2》 |
| 状态 | **建议（待前端负责人确认）** |
| 编制角色 | 产品通 + UI 规范建议 |

> **Why**：T1~T5、T2、T8 已给出技术栈、契约、安全与验收标准，但**前端没有统一的视觉与交互基线**，会导致后台与门户"各写各的"、组件重复、等保合规项（脱敏/密码强度/锁定提示）遗漏。本文件定义设计 Token、两套布局、路由映射、逐模块线框与状态/合规规范，让前端按图施工。

---

## 一、设计原则

| 原则 | 说明 |
|---|---|
| 效率优先 | 表单密集型后台，操作路径 ≤ 3 步；高频操作固定顶部/右侧 |
| 一致可预期 | 全局组件统一（表格/表单/弹窗/抽屉），交互不 surprise |
| 信息分层 | 列表→详情→抽屉三级；详情用标签页承载多维度 |
| 合规可视 | 脱敏、权限、审计、锁定等安全状态在 UI 上显式表达 |
| 复用中台 | 与合同系统共用 Element Plus 主题、权限框架、布局组件 |

---

## 二、技术 & 组件基线（对齐 T1）

| 维度 | 选型 |
|---|---|
| 框架 | Vue 3 + Vite + TypeScript |
| UI 库 | Element Plus（后台）+ 同库轻量定制（门户） |
| 状态 | Pinia |
| 路由 | Vue Router（后台 `/`、门户独立域 `/portal`） |
| 请求 | Axios 封装（统一拦截 T4 响应体 `{code,message,data,traceId}`） |
| 表单 | 受控 + 校验规则集中；富文本用 Quill/Tiptap（跟进记录/沟通内容） |
| 图表 | ECharts（看板/中标率/评价统计） |
| 主题 | **浅色（Light）**，支持暗色后续扩展 |

---

## 三、设计 Token

### 3.1 色彩（浅色主题）

| 类别 | Token | 色值 | 用途 |
|---|---|---|---|
| 品牌主色 | `--brand` | `#1F5FD6` | 主按钮、激活态、链接 |
| 品牌浅 | `--brand-light` | `#E8F0FE` | 选中背景、标签底 |
| 成功 | `--success` | `#2BA471` | 成交/通过/生效 |
| 警告 | `--warning` | `#E6A23C` | 待审批/临期/低信用 |
| 危险 | `--danger` | `#D54941` | 流失/驳回/作废/锁定 |
| 信息 | `--info` | `#909399` | 次要文字 |
| 风险红（等保） | `--risk` | `#C0392B` | D 级客户强预警标识 |
| 文本主 | `--text-1` | `#1D2129` | 标题/正文 |
| 文本次 | `--text-2` | `#4E5969` | 辅助说明 |
| 文本弱 | `--text-3` | `#86909C` | 占位/禁用 |
| 边框 | `--border` | `#E5E6EB` | 分割线/输入框 |
| 背景 | `--bg` | `#F5F6F8` | 页面底色；`#FFFFFF` 卡片 |

> 脱敏展示：敏感字段按角色不可见时显示 `****` 或 `138****8000`（见第九章）。

### 3.2 字号 / 间距 / 圆角

| 阶梯 | 值 | 用途 |
|---|---|---|
| 标题 H1 | 20px / 600 | 页面标题 |
| 标题 H2 | 16px / 600 | 卡片/分区标题 |
| 正文 | 14px / 400 | 默认 |
| 辅助 | 12px / 400 | 提示、时间 |
| 间距栅格 | 8px 基准（8/16/24/32） | 布局留白 |
| 圆角 | 4px（控件）/ 8px（卡片） | — |
| 阴影 | 卡片 `0 1px 4px rgba(0,0,0,.06)` | 悬浮微反馈 |

### 3.3 图标

- 统一用 `@element-plus/icons-vue`；业务图标（客户/商机/报价/投标/备案）补充一套 24×24 线性图标。
- 状态用色彩 + 图标双编码（如作废=红色 circle-close）。

---

## 四、布局系统

### 4.1 后台布局（内部管理后台）

```
┌───────────────┬──────────────────────────────────────────────┐
│  Logo/系统名   │  顶部栏: 全局搜索 │ 消息(站内) │ 用户/角色切换  │
│  ───────────   ├──────────────────────────────────────────────┤
│  一级导航       │  面包屑: 客户管理 / 客户列表      [标签页…]     │
│   ·客户管理     │──────────────────────────────────────────────│
│   ·商机管理     │                                              │
│   ·报价管理     │           内容区（路由出口）                  │
│   ·投标管理     │           列表 / 详情 / 表单 / 看板            │
│   ·客户门户     │                                              │
│   ·系统设置     │                                              │
│  (折叠按钮)     │                                              │
└───────────────┴──────────────────────────────────────────────┘
   左侧固定 220px（可折叠至 64px）   顶部 56px   内容区滚动
```

- 列表页标准三区：**筛选区（上，可折叠）+ 操作区（右/上）+ 表格区（下）**。
- 详情页用**标签页**：基本信息 / 干系人 / 商机 / 项目 / 沟通 / 日志。
- 新建/编辑用**抽屉 Drawer（右滑，宽 720px）**或**分步向导**（备案用 5 步）。

### 4.2 客户自助门户布局（独立域，对外）

```
┌──────────────────────────────────────────────────────────┐
│  品牌区: Logo + 系统名          导航: 我的项目│报告│备案│反馈  登录/注册 │
├──────────────────────────────────────────────────────────┤
│  内容区（卡片化、留白更大、弱化管理感）                        │
│   门户首页: 欢迎语 + 授权项目概览 + 快捷入口                    │
├──────────────────────────────────────────────────────────┤
│  页脚: 保密声明 │ 备案号 │ 客服                               │
└──────────────────────────────────────────────────────────┘
   登录/注册为独立全屏页（无顶部导航），HTTPS 强制
```

> 门户与后台**视觉区分**：门户更轻量、留白大、品牌友好；后台密集、效率导向。

---

## 五、导航与路由映射（对齐 T4）

| 一级菜单 | 二级 / 页面 | 路由 | 对应功能 |
|---|---|---|---|
| 客户管理 | 客户列表 / 新建 / 详情 | `/customer/list` `/detail/:id` | CM-001/002 |
| 客户管理 | 信用等级 | `/customer/credit` | CM-003 |
| 商机管理 | 商机看板 / 列表 / 详情 | `/opportunity/board` `/detail/:id` | BM-001/002 |
| 报价管理 | 报价列表 / 新建 / 详情 | `/quotation/list` `/detail/:id` | BM-003 |
| 投标管理 | 投标列表 / 详情 | `/bid/list` `/detail/:id` | BM-004 |
| 客户门户 | 账号开通 / 权限 | `/portal/account` | CP-001 |
| 客户门户 | 备案审核 | `/portal/filing` | CUS-004 |
| 客户门户 | 反馈与评价 | `/portal/feedback` | CP-004 |
| 通用 | 审批中心 | `/approval/center` | 全部审批流 |
| 通用 | 附件 / 枚举 | `/common/attachment` `/enum` | 通用能力 |
| 系统设置 | 角色权限 / 日志 | `/setting/rbac` `/log` | 全局 |

> 门户侧路由独立域：`/portal/login` `/register` `/my-projects` `/report-apply` `/filing` `/feedback`。

---

## 六、页面原型（线框）

### 6.1 客户列表页（CM-002）

```
┌ 筛选[名称▾][类型▾][行业▾][信用▾][状态▾][搜索] [查询][重置][+新增][导出] ┐
├───────────────────────────────────────────────────────────────────┤
│ □ 客户编号    客户名称        类型    行业    信用  状态   最近跟进  操作 │
│ □ KH20260701 某银行          业主    金融    A     成交   06-30    [详情][商机]│
│ □ KH20260702 某政务云        政府    政府    B     在跟   07-01    [详情][商机]│
│   …（虚拟滚动，100+ 并发）                                    [分页]   │
└───────────────────────────────────────────────────────────────────┘
   快捷筛选标签: 全部│重点│新客│高价值│待跟进（顶部 chip）
```

### 6.2 客户详情（标签页，CM-001）

```
标签: [基本信息][干系人][商机历史][项目历史][沟通记录][变更日志]
──── 基本信息 ────
  名称: 某银行   类型: 业主   行业: 金融   状态: 成交   信用: A⚠(若D级红标)
  统一信用代码: 91110000********（非财务角色脱敏）
  财务: [查看需权限] 收款账户 6217****1234（脱敏）
[编辑][查重][合并][作废(软删确认弹窗)]
```

### 6.3 商机看板（BM-002）

```
┌ 初步接触 │ 需求沟通 │ 方案制定 │ 报价 │ 投标 ┐
│ [卡][卡] │ [卡][卡] │ [卡]     │ [卡] │ [卡] │   ← 拖拽改阶段
│ 商机名/金额/负责人/下次跟进                     │
└──────────────────────────────────────────────┘
   点击卡片 → 详情抽屉: 跟进记录 + [+跟进][阶段推进(触发审批)][转合同][流失]
```

### 6.4 报价单编辑（BM-003）

```
左: 基本信息(关联商机/客户/日期/有效期30d)
右: 明细表格[服务项目|内容|数量|单价|折扣|金额] [+行]
    付款条款[阶段|比例|触发条件]（默认 2 段，可+）
底部: [保存草稿][提交审批]  → 审批流节点可视化(销售总监→财务经理)
   低于限价时: 红条提示"+分管领导"节点
```

### 6.5 投标详情（BM-004）

```
标签: [基本信息][标书][保证金][开标][中标/落标]
保证金卡: 金额/缴纳/凭证/预计到期[提醒] 状态:未退│已退
开标: 时间/单位/报价/排名
中标: [上传中标通知书][推送合同(对齐T5 B)]  落标:[原因][退保证金申请]
```

### 6.6 门户：登录 / 注册（CP-001）

```
        [Logo] 客户自助门户
  ┌─────────────────────────┐
  │ 登录名 ____________      │
  │ 密码   ____________      │
  │ [验证码] [发送站内验证码] │   ← 本期站内验证码(2FA预留)
  │ [登录]                   │
  │ 邀请码注册 →             │
  └─────────────────────────┘
   失败≥5次: 红字"账号已锁定15分钟" + 倒计时
   密码强度条: 弱/中/强（≥8位含四类）
```

### 6.7 门户：我的项目 + 报告申请（CP-002/CP-003）

```
我的项目: 卡片列表(项目/合同/状态/进度条) → 点击看时间线
报告申请: [选择项目][报告类型▾][原因][接收邮箱] [提交] → 推下游审批(T5 E)
         已发放: [下载](72h有效, 异常下载冻结提示)
备案: 5 步向导(进度指示) → 提交锁定(管理员解锁)
反馈/评价: 类型▾+项目+描述 → 24h响应提示; 评价4维星级
```

---

## 七、全局组件规范

| 组件 | 规范 |
|---|---|
| 表格 | 虚拟滚动；列宽可拖；导出；行操作收进 `...` 菜单；空数据显空态插画 |
| 表单 | label 右对齐；必填红 `*`；错误内联；提交防重复；富文本限长 |
| 详情 | 抽屉(720) 或 新标签页；大对象用折叠 |
| 弹窗 | 危险操作(作废/合并/流失) 必须二次确认 + 原因必填 |
| 步骤条 | 备案 5 步、审批流节点可视化 |
| 看板 | 拖拽改阶段，实时乐观更新 + 失败回滚 |
| Toast | 成功/警告/错误三态；错误带 traceId 便于排查 |
| 权限无杈 | 无权限按钮禁用灰显 + hover 提示"无权限"；越权访问跳 403 页 |

---

## 八、状态与反馈

| 场景 | UI 处理 |
|---|---|
| 加载 | 骨架屏（列表）/ 局部 spinner（按钮）；首屏 ≤ 2s 目标 |
| 空态 | 插画 + "暂无数据" + 引导操作 |
| 错误 | 全局错误页 + traceId；接口错误映射 T4 错误码表 |
| 权限拒绝 | 403 页 / 按钮灰显 |
| 审批中 | 节点高亮 + 当前处理人 + 倒计时（临期黄标） |
| 临期提醒 | 报价失效/保证金到期 黄色 warning 角标 |

---

## 九、合规与等保 UI 要求（对齐 T2）

| 要求 | UI 落地 |
|---|---|
| 敏感字段脱敏 | 非授权角色显示 `****` / `138****8000`；财务区加"查看需权限"锁 |
| 密码强度 | 注册/改密实时强度条（≥8 位含大小写+数字+特殊字符） |
| 登录失败锁定 | 失败 5 次红字锁定 + 15 分钟倒计时 |
| 双因素 | 本期站内验证码输入（短信预留）；2FA 开关标识 |
| 会话超时 | 30min 无操作弹窗续期；过期跳登录 |
| 审计可见 | 变更日志 tab 全量展示 who/old→new/time |
| D 级预警 | 客户/商机页红色 `⚠ 风险` 强标识 |
| HTTPS | 门户全站强制；地址栏锁标 |
| 邀请码/链接 | 一次有效提示；过期提示重新获取 |

---

## 十、响应式与多端

| 端 | 策略 |
|---|---|
| 后台 | 桌面优先（≥1280px）；侧栏可折叠；平板降为抽屉 |
| 门户 | 兼容移动端（≤768px 单列）；适配飞书/企微内嵌 WebView |
| 大屏 | 看板/中标率大屏可选（后续） |

---

## 十一、组件复用清单 & 待确认

| 复用项 | 说明 | 状态 |
|---|---|---|
| 布局组件 | 后台壳 + 门户壳 | 新建（可复用合同系统） |
| 表格/表单封装 | 含分页/导出/校验 | 新建 |
| 审批流展示 | 节点可视化组件 | 依赖 T1 审批引擎选型 |
| 富文本编辑器 | 跟进/沟通 | 选型待定 |
| 脱敏指令 | `v-desensitize` 按角色 | 新建 |

**待确认**
1. 主色 `#1F5FD6` 是否采用，或沿用集团 VI 色。
2. 审批引擎选型（影响审批流组件实现，见 T1）。
3. 门户是否需独立设计语言（目前建议轻量复用 Element Plus）。
4. 富文本编辑器选型（Quill/Tiptap）。

---

## 十二、Non-goals（本期不做）

- 不接 IM/邮件/短信三方 UI（仅站内消息中心）。
- 不生成测评报告文件预览（CP-003 仅下载通道）。
- 不做项目执行管理 UI（CP-002 仅展示快照）。
- 不做暗色主题（浅色优先，后续扩展）。

---

**文档结束**
