# 客户与商机业务域开发状态

更新时间：2026-08-19

状态口径：

- **核心纵切面已实现**：需求的主要状态机、持久化、HTTP 路由和当前前端入口已贯通，但仍需真实外部系统/UAT/非功能门禁。
- **部分实现**：已实现可运行子集，需求文档中仍有明确业务能力缺失。
- **未实现**：尚无生产代码，原型展示不计入实现。

数据库验证口径：当前源码 migration plan 校验结果为 CRM 47 个文件至 `000075`，combined checksum 为 `sha256:994e286df8a5c8152bfff9498d449a089a5ce029cbed47b8314f36f3607afef7`；Portal 30 个文件至 `000077`，combined checksum 为 `sha256:c3f82ff64488ccb9b0218f3594294ae906647d59dd958cd49f454023e9a41299`。上述仅证明文件归属、顺序和 checksum 一致，不代表已在 MySQL 全新库或真实存量库执行。此前 2026-08-02 的空库/集成验证仍只覆盖 CRM 44 个文件至 `000071`、Portal 27 个文件至 `000070`；新增迁移必须补做空库、增量升级、在线 DDL、metadata lock、回滚和生产规模性能验证。此前合同仓库执行至 `000010` 创建 15 张业务表，另有 2 张 migration metadata 表。

> **范围说明（2026-08-05，已确认）**：客户自助门户**不涉及与公安相关的对接**。下述“公安提交 Worker / 回执”为超出门户范围的既有实现，按边界保留，门户验收不包含公安提交；对应需求文档 CUS-004 已按此口径更新。

最新外部文件/公安边界：`000063` 已把 ISSUED 回调内的外部 Ingest 拆为加密描述符异步作业。回调只原子进入 `INGEST_PENDING`；租约 Worker 以稳定事件号调用 provider-neutral Ingest，只有完整对象版本、AES-256-GCM、`CLEAN` 扫描证据和 SHA-256 同时成立才发布 `ISSUED`、文件与通知。`000066` 增加备案提交中/失败状态和不可变回执；公安提交 Worker 复核快照及材料证据，以稳定事件调用 Provider，只有经校验回执的密文、摘要、状态和 outbox 原子提交成功才进入 `SUBMITTED`。正式 Provider 未交付时两条链路都失败关闭，不生成伪文件或伪回执；新增迁移已包含在上述空库验证中。

## 逐需求状态

| 需求 | 状态 | 已实现 | 尚未实现或待联调 |
| --- | --- | --- | --- |
| CM-001 客户信息录入与维护 | 部分实现 | 创建、查重、更新、联系人事务更新、加密/脱敏、字段审计、作废/恢复、客户沟通记录、乐观锁；交互式创建强制操作人绑定的持久幂等键，客户号/客户/联系人/脱敏响应快照/审计单事务提交，敏感请求值先 HMAC 后仅保存最终摘要；幂等表唯一键和信用代码唯一键两种并发先后均只在完整 winner 绑定校验通过后重放；前端页面内模糊失败复用键，已实际发送的未确认重试不会被查重预检拦截；疑似重名覆盖为目录内 HIGH 权限，仅 `sales_director/customer_admin` 可携原因执行，普通 `sales` 失败关闭；关键干系人和信息系统子表、按数据范围读取、ACTIVE+客户版本全量维护、干系人联系方式 AEAD/脱敏、只含脱敏值的双审计、等保保护等级白名单（与信用评级无关）及前端按需页签；客户合并支持双客户数据范围、ACTIVE/双版本校验、客户和源商机固定顺序行锁、商机创建/客户沟通/邀请本地写入并发互斥、操作者+载荷幂等、单事务迁移现有联系人/干系人/信息系统/沟通/无合同商机/邀请、MERGED 映射、字段审计/合并日志/outbox 和前端核对向导；Portal 身份映射、补偿任务或已关联合同商机无法安全迁移时结构化阻断；Excel 导入已实现服务端安全 OOXML 解析、固定表头/行校验、脱敏预览、加密命令、租户+操作人范围、可接管租约、逐行独立事务、幂等回放、RFC 4180 错误报告和前端三阶段向导 | 导入的真实病毒/内容扫描适配器未接入，当前启动后预览明确返回 503；完整导出、财务扩展（字段归属与查看权限尚未签版）、Portal 跨库身份改绑编排、合同客户重绑契约、外部报价/投标关联迁移（本地暂无可靠关联 ID）、合并 outbox 消费者、6 年归档任务；`000034/000044` 上一版本升级、在线 DDL 和存量数据验证 |
| CM-002 客户查询与检索 | 部分实现 | tenant 与 SELF/ORG/ALL 数据范围；客户号/名称、类型、行业、区域、负责人、状态、创建时间和最近跟进时间组合筛选；NEW 近 30 天、WON、FOLLOWUP_DUE 快捷筛选；最近跟进与有效商机金额汇总、5 字段白名单稳定排序、严格分页/RFC3339/未知参数失败关闭；URL 可复现筛选、列表/卡片切换；基本信息、脱敏联系人、本地商机、沟通和操作日志按需页签及 IDOR 防护；项目历史通过独立 `portal.project_history.read` Client Credentials 读取 Portal 本地投影并提供真分页，严格区分源更新时间、快照写入时间和客户同步链路最后成功时间，从未成功同步时安全标记过期 | 重点客户无权威分类字段，KEY 明确返回 503 且前端禁用；项目历史适配器的正式 OAuth Client、scope、网关和项目同步环境待联调，默认关闭时返回 503；XLSX Worker、加密对象存储和短效下载未接入，导出鉴权后返回 503；全文检索、真实 MySQL 集成和 100+ 并发性能验收 |
| CM-004 门户账号与注册链接 | 核心纵切面已实现 | 一次性 256-bit token、2 小时有效、重发撤销、统一信封 verify/consume、Portal→CRM OAuth 机器调用与防重放、OIDC sub 匹配、审计、补偿任务落库；consume 在单事务内按 tenant/customer/contact/platform user/Portal account 完整绑定并锁定邀请与身份链接，原子收敛邀请 `USED`、CRM 链接 `ACTIVE` 和审计，精确重放幂等；生成接口强制 actor/customer/request 绑定的 `Idempotency-Key`，首次外部写前落加密恢复 saga，平台预置、角色分配、Portal mapping、邀请提交按阶段续跑且每步使用稳定远程键；CRM 已接精确 `external_user.provision`/`application_role.assign`/`application_role.revoke`；独立禁用访问命令按客户锁和身份链接 ID/版本建立正向 Saga，Portal 禁用映射/全会话退出、CRM 链接终态化/撤销待用邀请、基础平台角色回收依次收敛；DISABLED 栅栏阻止旧开通/补偿复活；请求和独立 SKIP LOCKED Worker 共用有限租约，稳定远程键、退避、8 次死信和事务审计；Portal 业务幂等账本、审计失败回滚，开通/禁用 route 分别绑定精确机器 client subject；状态 API 和前端按开通/撤销/禁用权限拆分读取、高风险确认/原因/稳定键/重试/死信可观测均已接入，且不泄露平台 subject；本地已通过 `cmd/seed-portal-invite` 完成 CRM→平台→Portal 全链路预置（外部用户+登录账号+`portal_customer`+身份映射+激活链接）验证 | 正式账号激活流程（管理端初始化密码）、HTTPS/OAuth Client 证书/网关和生产 E2E；预置不创建口令/凭据，激活前不能登录；`000064/000069/000070/平台000069` 上一版本升级、在线 DDL、生产 Worker 性能与 UAT |
| BM-001 商机创建与维护 | 部分实现 | 创建、列表、详情、主档更新、作废/恢复和依赖阻断；可信报价投标回调在阶段事务内保存只读快照；主动查询使用精确 `opportunity.status.read`、独立 OAuth、强制 HTTPS、禁止重定向、私有 CA/mTLS、严格 JSON/envelope 和最小 DTO；服务端只接受部署期固定 HTTPS 报价/投标地址并签发最长 5 分钟、含租户/商机/客户/用途/nonce 的 HMAC 上下文，前端已接“生成报价/发起投标”，不接受浏览器指定 URL；已签约且已验证关联合同的 outbox、合同核对与唯一权威关联；商机团队任期、列表/看板、阶段历史、跟进、预警、负责人通知与前端入口；平台负责人目录正式契约已签版并接通：CRM `GET /api/v1/owner-directory` 只返回内部有效 OIDC `sub` 与其 CRM 授权组织成员关系，客户/商机创建、主档更新和商机换负责人均按该权威配对校验，目录未配置/不可达/无权限时严格失败关闭，前端 `OwnerSelector` 按人员→组织联动选择 | 外部 IM/邮件/短信、附件正式对象存储/扫描；报价投标正式 Client、mTLS 证书、固定 URL、外部验签/nonce 防重放；合同上一版本 migration、正式网关、多实例和性能验收 |
| BM-002 商机跟进与阶段管理 | 部分实现 | 七阶段看板、无需审批的即时变更、历史、跟进、可信回传、数据库级防重/乱序审计、最新外部状态只读 API；可信 G 回传快照中“最新仍有效的已通过报价”已与商机预计金额精确比较并展示非阻断提示；可选主动查询会在父商机数据范围验证后读取外部权威快照；终态待办、阶段预警、站内消息和合同归属 HTTP 适配器 | 报价/投标和合同适配器正式环境；正式主动查询的“最新已通过报价”专用权威契约；预警/通知的团队与管理层收件人仍依赖外部业务系统目录，负责人目录不替代；外部短信/邮件/IM；`000047` 上一版本升级与生产性能 |
| TS-001 申请创建 | 核心纵切面已实现 | 申请校验、编号、商机快照、HTTP 路由；创建幂等重放会先校验父商机可见性，并绑定操作人、商机和规范化载荷，快速、事务及唯一键竞态回收均不会返回其他账号的申请；新增 `000072` 多实例持久心跳，独立售前投递 Worker 只有在成功读取 outbox 后才更新证据，CRM 首次创建在事务外/内双重检查任一新鲜实例，无记录、过期或查询失败返回 503，避免新任务永久停在 `APPROVAL_STARTING`，既有成功幂等重放仍可读取；会话能力接口复用同一真实证据且不泄露 Worker 身份；前端按与服务端一致的规范载荷在页面生命周期复用显式幂等键，只在成功后清除，并阻止重复点击；联系电话普通详情只返回掩码，独立按需端点同时校验 CRITICAL 权限、tenant 与当前/历史真实指派或明确管理角色，显式拒绝 auditor；解密及掩码绑定后先写无明文隐私审计，审计/解密失败统一关闭，响应 no-store；前端仅在权限和服务端关系能力均成立时显式查看，关闭或切换详情即清除内存明文 | 整页重载后的结果歧义仍须先查服务端状态；正式审批/PMS 环境、Worker 监控告警和全链路 UAT；基础平台已交付直接有效组织 claims，但角色 `ORG` scope 模式与售前任务组织归属契约尚未签版，管理角色目前仍按已确认的租户全量范围处理 |
| TS-002 两级审批 | 核心纵切面已实现 | 两级审批本地投影、可信回调、启动/动作异步投递；独立当前任务适配器以 `engine_instance_id + node + authenticated approver`、精确只读 scope、独立 OAuth/mTLS、强制 HTTPS、禁止重定向和严格 JSON 查询权威待办；独立 `available-actions` 只有解析成功且无待确认动作才发布操作，动作时再次解析，浏览器 DTO 不接收 `engine_task_id`；待确认绑定不可被第二动作覆盖；`000065` 保存 instance/sequence，回调及重放严格绑定实例、任务、序号、节点、审批人、结果、意见与发生时间；人工命令具备操作人/父申请/动作/载荷幂等和节点推进后精确重放 | 审批引擎正式当前任务/动作 OpenAPI、OAuth Client、scope、网关、mTLS 和真实环境联调；角色 `ORG` scope 模式与任务组织归属仍未签版；`000040/000060/000065` 上一版本升级/在线 DDL |
| TS-003 人员指派 | 核心纵切面已实现 | 多人指派、改派历史、当前执行人集合；替换命令以父申请行锁和 actor-bound 持久幂等记录原子提交，前端显式重试键与规范化人员顺序对齐；每个新增/移出人员在同一事务追加不可变证据和只含证据 ID 的 outbox，精确重放不重复；专用通知 Worker 采用 SKIP LOCKED、租约接管、6 次退避/第 7 次死信，按 tenant 复核任务、指派、证据、稳定事件 ID 和时间后幂等投影个人站内信；个人 API 分离 user/person 收件人命名空间且不受 SELF/ORG/ALL 扩大，前端点击后通过真实售前详情接口复核数据范围；有效 PMS 人员池筛选、最小 DTO/同步时间；手动异步入队与 6 小时调度、持久幂等合并、OAuth `technician.read`、完整快照事务 upsert/停用缺席、旧缓存保留、联系方式 AEAD；前端人员选择器和新增/保留/移出差异 | 基础平台须正式保证 OIDC `person_id` 与 PMS `person_id` 权威映射；缺失时通知失败关闭且不回退 `user_id`；PMS 正式环境联调；组织 claims 已交付，但角色 `ORG` scope 模式/任务组织归属契约未签版；T5 无时区 legacy datetime 升级 RFC3339；权威空人员池缺乏显式 empty 证明，当前安全拒绝空快照；`000040/000042` 真实 MySQL/E2E/多实例/性能验收；外部 IM/邮件未接入 |
| TS-004 过程记录与状态机 | 核心纵切面已实现 | 进度追加、状态约束、取消；取消命令以父申请行锁、操作人和规范化载荷持久幂等，前端失败重试复用显式键；进度新增已实现任务/当前指派事务锁、乐观版本、纯文本与 HTTPS 长度边界、租户级持久幂等和跨任务唯一键竞态回收；进度、逐收件人不可变证据和 opaque-reference outbox 同事务，可信接收人限申请人 `user_id` 与发生时其他当前执行人 `person_id`，两命名空间分离且作者按同命名空间排除；独立过程通知 Worker 逐条随机 fencing token 领取，重读 tenant/进度/任务/发生时指派任期，有限重试/死信和稳定事件幂等投影个人收件箱；状态/审批/指派/进度/有效工时的稳定 keyset 聚合时间线；绑定租户+申请的 HMAC cursor、父资源数据范围复核和最小 DTO；独立服务端权威 `available-actions`；前端中文时间线、加载更多、HTTPS 链接复核、操作后三区域刷新和过程通知详情跳转 | 基础平台已实现显式 `pms_person_id` 维护及 OIDC `person_id` 签发，仍需正式 PMS 目录数据绑定/E2E；缺绑定的账号继续安全关闭；组织 claims 已交付，但角色 `ORG` scope 模式、任务组织归属和管理层收件人目录尚未签版，当前不猜测管理层过程通知；外部 IM/邮件/短信未接；`000035/000036/000040/000051` 上一版本升级验证；生产规模 UNION/通知投影压测与在线 DDL |
| TS-005 工时登记与自动完成 | 核心纵切面已实现 | 小时/人天换算；单人首笔、多人人人首笔自动完成；工时幂等绑定账号、人员、任务和规范化载荷，并先锁定父任务、复核当前执行人；精确重放在自动完成后仍可返回原工时，跨账号/任务和竞态路径不泄漏旧对象；前端按与服务端一致的 RFC3339 时间、两位数值、单位/地点/内容/备注/版本规范载荷在页面内复用显式键，真实发送前标记、仅确认成功清除且不把账号/person_id 放入请求体；乐观锁 | 平台映射/claims 代码已完成，仍需正式 PMS 人员数据绑定和 OIDC E2E；缺 `person_id` 的账号安全关闭；整页重载后的模糊结果仍须查询服务端状态；真实业务 UAT |
| TS-006 工时推送 PMS | 核心纵切面已实现 | 独立 Worker、独立精确 `presale.worklog.write` OAuth Client、强制 HTTPS/可选私有 CA 与 mTLS、禁止重定向、时间戳/随机 nonce、`worklogId` 业务幂等键及严格成功信封/回执绑定；SKIP LOCKED 租约、6 次重试、死信、人工恢复 | PMS 正式 OpenAPI/端点/Client/证书联调和生产监控告警 |
| TS-007 列表与角色视图 | 核心纵切面已实现 | 列表、详情、工时、角色和数据范围查询；列表/详情/状态看板/筛选选项统一使用同一数据域：销售仅本人申请，具有权威非空 `person_id` 的主体仅凭真实当前/历史指派关系获得对应任务，多角色取并集，未指派资源失败关闭；PMS 指派业务角色不再猜测等同 CRM OIDC 角色；后端看板固定 7 状态、每列默认 20/最多 50 条且返回 total；选项只从调用者可见的本地权威数据产生，每类最多 100 条并显式标记截断；补齐任务号/商机/申请人/执行人/状态/场地/紧急度/时间区间/逾期/PMS 推送状态和稳定排序；前端已接服务端选项、列表/只读看板切换、中文枚举、完整 URL 状态和过期响应防护 | 平台权威映射/claims 已实现，仍需生产 PMS 数据绑定与 OIDC E2E；缺 `person_id` 的执行人视图安全关闭；`000037` 真实 MySQL 8/`EXPLAIN ANALYZE`；生产数量 P95 ≤ 2 秒；Vue 组件挂载与浏览器 E2E 验收 |
| TS-008 超时预警 | 核心纵切面已实现 | 租户规则与版本历史、UTC 状态起算、数据库租约和主键游标扫描；预警唯一键及 API 已显式区分 CRM `USER/user_id` 与 PMS `PERSON/person_id`，执行人、申请销售、团队负责人和节点 1 销售总监按真实来源投影；管理角色从未撤销且未过期的本地 CRM OIDC 会话角色目录解析，不当作 PMS 角色；浏览器列表/已读只精确合并 actor 的 user 与非空 person 身份，不受 SELF/ORG/ALL 扩大且不推断相等；状态/规则/接收人变化取消、outbox/未读投影、任务聚合标记、规则与预警前端；`000071` 将不可证明来源的旧收件人保留为不可访问 `LEGACY_UNKNOWN` 并取消旧 PENDING 投递；平台负责人目录正式契约已签版（`owner_directory.read` 机器 scope + CRM `owner-directory` 浏览器端点），提供内部有效 OIDC 用户与其组织成员关系 | 审批引擎未提供实际当前审批人目录，本地角色提醒不代表当前审批人已触达；只有已建立有效 CRM 会话的管理角色能进入当前本地角色投影，负责人目录不替代 PMS 当前审批人；触达率指标、上一版本在线迁移和生产规模性能验收 |
| TS-009 售前投入报表 | 部分实现 | 标准小时 summary/trend/distribution 实时查询；有效工时、历史人员/部门快照、自动完成、活跃商机覆盖率、PMS 最终成功率口径；UTC/RFC3339 半开时间边界；ALL/ORG/SELF 数据范围和组织/人员/商机筛选；KPI、分布图和等价数值表前端；`000067` 日报事实表及独立聚合 Worker 已按 tenant/UTC 日窗口事务性可重跑，具备租约、运行成功/失败证据、历史姓名/部门快照与 PMS outbox 分母，已在 MySQL 8.4 用样例工时连续重跑验证不重复 | 基础平台/PMS 生产 person 映射环境联调；异步导出依赖 Worker/对象存储/受控下载，当前规范返回 503；聚合投影新鲜度/降级策略尚未签版，在线 API 暂不读取投影；大数据量性能验收 |
| TS-010 商机关联展示 | 核心纵切面已实现 | 组合路由先复核商机数据范围，再按租户+商机返回售前最小摘要；详情标记独立要求 `presale.read` 且限管理角色、申请人或具有权威 `person_id` 的真实历史执行人，PMS 指派角色不猜测为 CRM OIDC 角色；前端商机详情已接分页摘要、空态、仅摘要阻断、最多 100 条查看及商机 ID 预填只读的售前发起入口；售前变化不修改商机阶段 | 平台映射/claims 已实现，仍需生产 PMS 数据绑定与 OIDC E2E；缺 `person_id` 的执行人详情入口安全关闭；真实基础平台权限目录联调、生产规模性能和浏览器 E2E |
| CP-001 Portal 访问控制 | 核心纵切面已实现 | 独立服务/schema、OIDC Code+PKCE、服务端会话、安全 Cookie、邀请激活、sub/customer 绑定、Portal→CRM client_credentials 机器 Client、Origin/CSRF | 基础平台、真实 MySQL、多实例端到端联调 |
| PO-08 账号安全 | 核心纵切面已实现 | 会话短 TTL、15 秒 UserInfo 授权复核、当前账号活跃会话查询/即时撤销、当前会话 Cookie 清理、登录成功/失败/sub 不匹配/权限失效/会话撤销事件、异常事件幂等确认、脱敏账号/IP/设备摘要、可信配置的平台安全中心跳转和前端页面 | IP 属地需可信 GeoIP/网关来源；平台全局登录失败、账号禁用、密码、MFA、找回仍归基础平台并待真实环境联调 |
| CP-002 项目进度查询 | 部分实现 | tenant/customer 范围的项目列表、真实详情 DTO、总体进度/阶段/预计结束/延期、里程碑、团队和脱敏联系方式；详情与动态独立真分页并严格校验 query，不透明项目 ID 可包含编码斜杠；独立快照同步 Worker；异步 PDF 捕获不可变快照，支持租约接管并以账号绑定一次性凭证受控下载；项目经理站内会话以权威 `manager_portal_account_id` 绑定，客户/经理路由在事务内复核当前映射，具有持久幂等、限流和换绑收敛；消息历史使用不透明 exclusive keyset 游标，真正展示给当前账号的接收消息才写逐消息回执，超过 100 条或并发新增时不会把未展示消息标已读或产生 OFFSET 漂移；前端支持发送、加载更早消息和逐页回执 | 上游代理必须原样保留编码斜杠；项目服务须正式同步 `manager_portal_account_id` 并完成经理机器客户端/工作台 UI 联调；部署需配置已签版 CJK TTF 与可写 pdfcpu 配置目录，未配置时 Worker 启动失败且不会伪造 PDF；上一版本升级、浏览器 E2E 和性能验收 |
| CP-003 电子报告申请与发放 | 部分实现 | 报告申请/列表/详情、事务 outbox、独立 OAuth 投递 Worker、SKIP LOCKED 租约、6 次重试/死信、审批投影、application JWT + 最小 scope + timestamp/nonce 回调认证、机器主体与请求体 tenant 强绑定、全生命周期回调幂等/乱序保护、客户/项目隔离和可信文件元数据白名单；申请输入完成规范化、邮箱/长度/控制字符校验，且父项目可见性先于幂等回放，回放/并发恢复强校验账号与父项目绑定；`000038` 提供不伪造历史的不可变状态事件及最小时间线 DTO；可信 Ingest 成功的 ISSUED 回调在文件、状态和事件事务内为申请账号创建唯一未读站内通知，Ingest 失败不通知；个人列表/未读数/幂等已读按 tenant+customer+account 隔离，首次已读追加不可变审计，前端进入真实报告详情；`000039` 提供最长 72 小时的账号绑定下载 grant、父报告行锁串行刷新、只存高熵 token 摘要、过期/冻结/撤销和 append-only 下载审计；可见报告的无效或非法长度 token 按账号+报告+小时有界审计，审计失败时下载失败关闭且不保存 token/hash，不存在/越权报告统一不透明 404；下载凭证不进 URL/请求体/Authorization/数据库明文；服务端在读取及分配缓冲前重新校验数据库文件元数据，在 50 MiB 上限内以最多 2 个并发缓冲复核真实字节数和 SHA-256 后才提交 200；`000041` 将报告申请/grant 操作人审计列及 GORM 元数据对齐 Portal 账号 128 字节上限；`000068` 将可信风险策略的冻结、拒绝审计和账号范围站内告警绑定到同一事务；客户前端可见 OPEN 安全提醒；独立 `portal.report.risk.manage` 机器接口按租户、版本、操作人、原因和幂等键执行复核，解冻只恢复尚未过期且无其他活动授权的原 grant，撤销重发不制造明文 token，只要求客户再次显式点击生成；所有复核追加不可变审计 | 项目服务正式联调；可信对象存储读取/信封解密、AES 文件加密和可追踪水印适配器未接，当前 `unavailableReportFileReader` 明确返回 503，不能视为下载闭环；正式 reader 必须遵守 `OpenVerified` 契约并在 context 取消后解除阻塞，生产网关仍须设置下载并发/内存保护；响应已提交后若完成审计失败只能告警、无法改写 HTTP 状态；可信风险网关/GeoIP 规则未接，未配置时继续失败关闭；`000038/000039/000041/000043/000068` 上一版本升级与在线 DDL 验证；真实 E2E/性能验收 |
| CUS-004 等保备案 | 部分实现 | OIDC tenant/customer 隔离、7 步 `2025.1` 安全字段子集、加密草稿、乐观锁、矩阵与跨域校验、不可变提交快照；材料受控上传/最终化/病毒扫描回调采用 provider-neutral 对象端口、对象版本/SHA-256/扫描凭证和精确 machine scope，提交前必须 `CLEAN`，Vue 向导已接真实上传并对未配置依赖明确 503；提交同步落仅引用快照的 `WAITING_CONTRACT` outbox；公安提交 Worker 原子激活 `SUBMITTING`、解密复核 snapshot/material 证据、按稳定事件幂等调用 Provider，七次失败转 `SUBMISSION_FAILED`；只有经校验回执的密文、明文摘要、备案状态和 outbox 原子提交成功才进入 `SUBMITTED`；管理员解锁与前端各状态只读语义 | 正式 2025 全字段/schema/模板与敏感分类、对象存储/扫描 Provider、PDF 渲染、公安请求/签名/回执 Provider、解锁审批、正式 scope；Provider 未签版前不提供会伪造成功的运行适配器；上一版本升级/E2E/性能 |
| CP-004-a 服务评价 | 核心纵切面已实现 | tenant/customer/OIDC sub 范围的完成项目资格、四维 1～5 分一次性评价、固定两位平均分、安全纯文本评语、actor+幂等键+载荷摘要、事务内项目行锁复核、审计；低分唯一告警、内部未读待办/outbox、独立 `portal.evaluation.read` 机器端列表与幂等已读；最小样本 5 的租户匿名汇总；项目详情前端可访问评分与提交后只读 | 项目系统正式状态枚举/同步联调，基础平台 `evaluation.create/read` 与 `portal.evaluation.read` 权限登记，管理层人员目录、外部消息适配、独立内部运营工作台和真实 MySQL/E2E/并发压测 |
| CP-004-b 客户反馈 | 核心纵切面已实现 | 异议/投诉/建议创建、本人列表/详情、补充沟通、解决后关闭、客户可见状态时间线；关闭及处理端状态动作使用租户级唯一、资源/actor/动作/载荷绑定的持久幂等键，精确重放不重复状态日志或 outbox，前端页面内复用关闭键；tenant/customer/sub 三重隔离且可见性先于重放；浏览器权限精确分为 `feedback.create/read/reply`，列表/详情、创建、补充/关闭路由与前端入口/加载/按钮一致失败关闭；客户与机器处理列表严格绑定 status/type/分页及绝对页码上限，详情和写动作拒绝 query，畸形编码、非法/未知/重复/溢出/超限参数在不触达仓储前失败，鉴权/scope/Origin/CSRF 保持优先；联系方式 AEAD 加密与脱敏；独立 `portal.feedback.manage` 机器处理状态机、审计/outbox；24 小时 SLA 独立 Worker、数据库租约、SKIP LOCKED、唯一升级和内部未读待办 | 处理端最终归属、独立客服运营工作台、基础平台 machine scope 正式登记、外部管理层通知、附件可信上传/对象存储/病毒扫描和真实 MySQL/E2E 联调；Portal 精确权限目前为源码契约测试，尚缺组件挂载级五种权限组合交互测试 |

## 工程与安全底座

- BM-001 商机可信附件纵切面已落地：只存对象引用，独立读/上传/下载权限，tenant 与父商机数据范围，严格文件元数据、20 MiB 上限、SHA-256、上传/最终化租约、扫描状态机、机器回调、审计和 `CLEAN` 才可受控下载。前端对 create/complete 分别保留页面内稳定幂等键；正式对象存储/病毒扫描适配器未联调，bootstrap 默认在任何上传会话写入前 503 失败关闭。`000049` 已通过 MySQL 8.4.11 全新 schema 验证，上一版本升级和生产适配器 E2E 仍待完成。

已实现：Go/Gin/GORM/MySQL 模块化后端；CRM、Portal、售前 Worker、项目同步 Worker、报告投递 Worker 和合同转交 Worker 独立进程；CRM 与 Portal 独立 OIDC Client/会话/Cookie；CRM/Portal 独立浏览器授权目录、稳定 catalog checksum、独立 `authorization.catalog.sync` 发布凭据、内嵌 Claims 兼容哈希、生产启动一致性校验及 CRM 角色权限精确并集校验；机器接口使用基础平台 application JWT 的只读 Ed25519 公钥、独立 issuer/audience、完整 claims 和数据库 nonce 防重放验证，不复用浏览器 OIDC JWKS；请求 ID、统一响应、数据范围、审计、敏感字段 AEAD/HMAC；CRM/Portal MySQL 健康检查和安全结构化访问日志（路由模板、稳定错误码、认证主体、状态/耗时/响应字节，明确不读取 query/header/body/Cookie/Token/IP/UA/panic 值）；CRM/Portal 当前生产 Gin 入口 OpenAPI 3.1 全量 path/method、安全方案、权限/scope、幂等头、请求体乐观锁和统一错误信封，并有解析真实路由源的防漂移测试/命令；Vue 3/Vite 管理端和 Portal 模块；明确的双 schema migration 清单。

尚未达到上线完成定义：当前入口 OpenAPI 已覆盖真实路由，但全部业务响应细分 schema、错误码示例和破坏性变更基线仍需继续细化；外部 Provider OpenAPI 仍等待各归属方正式签版；指标名称/标签/采集协议和告警、授权目录在真实基础平台环境的首次登记/发布验收、全量集成/E2E/Testcontainers、覆盖率门禁、性能压测、OWASP ZAP、安全与 IDOR 全量测试、备份恢复/回滚演练、真实外部系统联调和 UAT 尚未完成。`/metrics` 和独立 readiness 路径因基础设施暴露契约未确认而未猜测开放。

## 当前可运行入口

- CRM API：`go run ./cmd/crm-server`
- 合同转交投递：`go run ./cmd/contract-transfer-worker`
- 客户 Portal：`go run ./cmd/portal-server`
- 售前异步投递：`go run ./cmd/presale-worker`
- PMS 技术人员池同步：`go run ./cmd/presale-engineer-sync-worker`
- 售前指派站内通知：`go run ./cmd/presale-assignment-notification-worker`
- 售前过程站内通知：`go run ./cmd/presale-progress-notification-worker`
- 售前超时预警：`go run ./cmd/presale-alert-worker`

TS-008 售前超时预警 Worker 已纳入本地 Docker 编排：服务名为 `customer-presale-alert-worker`，可通过
`bash platform/scripts/docker-local.sh start-presale-alert-worker` 单独构建并启动；本地运行验证已确认容器保持运行且无启动错误日志。生产 Compose 使用同名独立服务。售前申请需求说明当前仅要求非空，不再设置 10～2000 字业务长度限制。
- 商机阶段预警：`go run ./cmd/opportunity-alert-worker`
- 商机负责人站内通知：`go run ./cmd/opportunity-owner-notification-worker`
- Portal 项目快照同步：`go run ./cmd/portal-project-worker`
- Portal 项目 PDF 导出：`go run ./cmd/portal-project-export-worker`
- Portal 报告审批投递：`go run ./cmd/portal-report-worker`
- 客户反馈 SLA：`go run ./cmd/portal-feedback-worker`
- Portal 邀请映射补偿：`go run ./cmd/portal-invite-compensation-worker`
- Portal 访问禁用恢复：`go run ./cmd/portal-access-disable-worker`
- OpenAPI 路由防漂移检查：`go run ./cmd/openapi-check`
- 演示数据初始化（幂等）：`go run ./cmd/seed-demo-data`
- 统一前端：在仓库 `frontend` 目录执行 `npm run dev`

环境变量分别见 `.env.example`、`.env.portal.example` 以及各进程对应的 `.env.*-worker.example`；数据库执行顺序见 `migrations/README.md`。
